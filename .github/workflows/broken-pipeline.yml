name: Broken CI Pipeline

on:
  push:
    branches: main  # Error 1: Should be [main]

jobs:
  test:
    runs-on: ubuntu  # Error 2: Invalid OS (should be ubuntu-latest)
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.9
      - name: Install deps
        run: |
          pip install -r requirements.txt
          pip install pytest
      - name: Run Tests
        run: pytest tests/ -v

  build:
    runs-on: ubuntu-latest
    # Error 3: Missing 'needs: test'
    steps:
      - uses: actions/checkout@v4
      - name: Build Image
        run: docker build -t myapp .

  deploy:
    runs-on: ubuntu-latest
    needs: test
    # Error 4: Needs build too (needs: [test, build])
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Deploy
        run: echo "Deploying..."